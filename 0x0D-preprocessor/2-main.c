#include <stdio.h>

/**
 * main - Entry point. Prints the name of the file
 * it was printed from, followed by a new line
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	puts(__FILE__);
	return (0);
}
