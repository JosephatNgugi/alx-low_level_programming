#include "main.h"

/**
 * _puts - Prints a string to stdout
 * @str: String to be printed
 */

void _puts(char *str)
{
	int i, length = _strlen(str);

	for (i = 0; i < length; i++)
	{
		_putchar(str[i]);
	}

	_putchar('\n');
}
